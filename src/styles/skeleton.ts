/** Shared pulse tokens for dashboard + public skeletons. */
export const skelPulse = "animate-pulse motion-reduce:animate-none";

export const skelBlock = `rounded-lg bg-black/[0.06] ${skelPulse}`;

export const skelLine = `rounded bg-black/[0.05] ${skelPulse}`;

export const skelCircle = `rounded-full bg-black/[0.06] ${skelPulse}`;

export const skelMuted = `rounded bg-black/[0.04] ${skelPulse}`;
